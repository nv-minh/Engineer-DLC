{
  "name": "edlc-monorepo",
  "private": true,
  "description": "EDLC monorepo — VS Code extension + core engine",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nv-minh/edlc"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "compile": "pnpm -r compile",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "package:extension": "pnpm --filter edlc package",
    "test": "pnpm --filter @edlc/core test",
    "edlc": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@vscode/vsce-sign",
      "keytar"
    ]
  }
}
